#!/usr/bin/make -f

export DEB_BUILD_MAINT_OPTIONS = hardening=+all

%:
	dh $@ --buildsystem=cmake --with=

override_dh_auto_configure:
	dh_auto_configure -- -GNinja -DTRANSMIT_BUILD_TESTS=ON

override_dh_auto_test:
	cd obj-* && ctest --output-on-failure
